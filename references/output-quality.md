# 输出质量规则

- 所有路径使用相对仓库根目录的正斜杠路径。
- Markdown 标题层级稳定，不跳级。
- 空占位符使用 `(暂无)`，整节为空时删除该章节。
- 代码块添加语言标识。
- 模块摘要面向 AI 快速定位，优先写职责、入口、依赖、修改提示。
- 不复制大段源码，只摘取关键片段，或用路径和行号定位。
- 如果上下文描述与代码分析冲突，说明冲突点并询问用户。
- 输出前检查生成文档路径、索引路径、模块类型、复杂度和未处理事项。
